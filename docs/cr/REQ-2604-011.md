## Change Request

- CR ID        : REQ-2604-011
- 레파지토리   : msys
- 요청 유형    : refactor
- 대상 스코프  : backend/services/utils
- 작업 유형    : 기능 작업
- 요청자       : 사용자
- 요청 날짜    : 2026.04.20

---

## 📊 현재 상태
Dashboard에서 CD400(100단위 Job)이 표시되어 사용자 혼란 발생. 
TB_CON_HIST에 오늘 날짜 데이터가 CD400만 존재하여 Dashboard에 표시됨.
동일한 100단위 제외 로직이 여러 파일(service/mngr_sett_service.py, routes/api/data_definition_api.py, service/data_definition_service.py)에 중복 구현되어 유지보수 어려움.

## ✅ 완료된 것
- [x] 원인 분석: TB_CON_HIST에 CD400만 오늘 데이터 존재, CD100/CD200은 데이터 없음, use_yn='Y' 확인 완료
- [x] utils/job_utils.py 신규 생성: should_exclude_job() 공통 함수 구현
  - CD900~CD999 범위 제외
  - 100단위(CD100, CD200, CD300, CD400 등) 제외
- [x] service/dashboard_service.py: get_summary()에 100단위 필터링 적용
  - use_yn 필터링 이후 100단위 제외 로직 추가
  - 로깅 추가: "[FILTER] After 100-unit exclusion: X job IDs remaining"
- [x] service/mngr_sett_service.py: _should_exclude_job() 메서드 → utils.job_utils.should_exclude_job import로 리팩토링
- [x] routes/api/data_definition_api.py: 중복 검증 로직 → should_exclude_job() 공통 함수 사용하도록 리팩토링
- [x] service/data_definition_service.py: 7개 중복 로직 → should_exclude_job() 공통 함수 사용하도록 리팩토링
- [x] 지침 수정: .clinerules/core/06.git-rules.md에 상세 커밋 메시지/CR 보고서 템플릿 추가 (e.txt 기반)

## 🔄 진행 중인 것
없음 (Build Mode 작업 완료)

## ❓ 미결 결정사항
- CD400 제외 후 Dashboard에서 정상적으로 사라지는지 테스트 필요
- CD101 성공률이 계속 정상 표시되는지 확인 필요
- 기존 기능 (mngr_sett, data_definition) 정상 작동 확인 필요

## 📋 다음 작업
- [ ] Dashboard 접속하여 CD400 미표시 확인
- [ ] CD101 성공률 100% 정상 표시 확인
- [ ] 기존 기능 (mngr_sett, data_definition) 정상 작동 확인
- [ ] Git commit & push

## ⚠️ 주의사항 / 이전 실수
- 공통 유틸로 분리하여 코드 중복 제거 (유지보수성 향상)
- 로깅 추가로 필터링 결과 추적 가능
- DashboardService에서는 TB_CON_HIST 기준으로 job_id 조회 후 use_yn 필터링 적용
- 100단위 제외는 use_yn 필터링 이후에 적용됨 (순서 중요)
- data_definition_service.py에서 그룹 코드 판별(cd_cl_number % 100 == 0)은 로직 유지, cd_number 판별만 공통 함수 사용

## 📁 관련 파일 경로

| 순번 | 구분 | 파일 경로 | 변경 유형 | 상세 내용 |
|------|------|-----------|-----------|-----------|
| 1 | Backend | utils/job_utils.py | 생성 | should_exclude_job() 함수 신규 구현. CD900~CD999 및 100단위 제외 |
| 2 | Backend | service/dashboard_service.py | 수정 | get_summary()에 100단위 필터링 로직 추가. import 추가 |
| 3 | Backend | service/mngr_sett_service.py | 수정 | _should_exclude_job() 메서드 → should_exclude_job import로 변경 |
| 4 | Backend | routes/api/data_definition_api.py | 수정 | 중복 검증 로직 → should_exclude_job() 사용하도록 리팩토링 |
| 5 | Backend | service/data_definition_service.py | 수정 | 7개 중복 로직 → should_exclude_job() 사용하도록 리팄토링 |
| 6 | Document | .clinerules/core/06.git-rules.md | 수정 | 상세 커밋 메시지/CR 보고서 템플릿 추가 (e.txt 기반) |
| 7 | Document | e.txt | 수정 | 현재 작업 내용 기록 |
| 8 | Document | dashboard_improvement_plan.md | 수정 | 작업 완료 상태 업데이트 |

## 📝 상세 변경 내용

### 파일: utils/job_utils.py (신규)

```python
"""
Job 유틸리티 모듈
Job ID 관련 공통 기능 제공
"""
import re


def should_exclude_job(job_id):
    """
    100단위 및 CD900~CD999 제외 여부 확인
    - CD100, CD200, CD300, CD400 등 100단위 제외
    - CD900~CD999 범위 제외
    
    Args:
        job_id: Job ID 문자열 (예: 'CD400')
        
    Returns:
        bool: 제외해야 하면 True, 아니면 False
    """
    if not job_id:
        return True
    
    job_id = str(job_id).upper().strip()
    
    # CD 패턴 매칭
    match = re.match(r'CD(\d+)', job_id)
    if match:
        cd_number = int(match.group(1))
        # CD900~CD999 또는 100단위 제외
        return (900 <= cd_number <= 999) or (cd_number % 100 == 0)
    
    return False
```

### 파일: service/dashboard_service.py

**Before:**
```python
# 3. Filter by use_yn = 'Y' from tb_con_mst
...
if not filtered_job_ids:
    return []

# 4. Combine historical data with today's schedule
```

**After:**
```python
# 3. Filter by use_yn = 'Y' from tb_con_mst
...
if not filtered_job_ids:
    return []

# 3.5. Exclude 100-unit jobs (CD100, CD200, etc.) and CD900~CD999
filtered_job_ids = [
    jid for jid in filtered_job_ids
    if not should_exclude_job(jid)
]
logging.info(f"[FILTER] After 100-unit exclusion: {len(filtered_job_ids)} job IDs remaining")

if not filtered_job_ids:
    return []

# 4. Combine historical data with today's schedule
```

**import 추가:**
```python
from utils.job_utils import should_exclude_job
```

### 파일: service/mngr_sett_service.py

**Before:**
```python
def _should_exclude_job(self, job_id):
    """설정 생성을 제외해야 할 job_id인지 확인합니다."""
    if not job_id or not str(job_id).strip():
        return True
    
    job_id = str(job_id).upper()
    
    # CD900~CD999 범위 제외
    if job_id.startswith('CD') and len(job_id) > 2:
        try:
            cd_number = int(job_id[2:])
            if (cd_number >= 900 and cd_number <= 999) or (cd_number % 100 == 0):
                return True
        except ValueError:
            pass
    
    return False
```

**After:**
```python
def _should_exclude_job(self, job_id):
    """설정 생성을 제외해야 할 job_id인지 확인합니다."""
    return should_exclude_job(job_id)
```

**import 추가:**
```python
from utils.job_utils import should_exclude_job
```

### 파일: routes/api/data_definition_api.py

**Before:**
```python
# CD900-CD999 범위와 100의 배수는 제외
cd_number = int(cd[2:]) if cd.startswith('CD') and cd[2:].isdigit() else None
if cd_number and ((900 <= cd_number <= 999) or (cd_number % 100 == 0)):
    return jsonify({"message": "CD900-CD999 범위와 100의 배수는 제외됩니다."}), 400
```

**After:**
```python
# CD900-CD999 범위와 100의 배수는 제외
if should_exclude_job(cd):
    return jsonify({"message": "CD900-CD999 범위와 100의 배수는 제외됩니다."}), 400
```

**import 추가:**
```python
from utils.job_utils import should_exclude_job
```

### 파일: service/data_definition_service.py

**변경 1 - create_data 메서드:**
```python
# Before
if cd_number and not (cd_number % 100 == 0):
    self.create_mngr_sett(cd)

# After
if not should_exclude_job(cd):
    self.create_mngr_sett(cd)
```

**변경 2 - update_data 메서드 (그룹 코드 확인):**
```python
# Before
if cd_number and cd_number % 100 == 0:

# After
if cd_number and should_exclude_job(cd):
```

**변경 3 - update_data 메서드 (상세 객체 활성화):**
```python
# Before
if cd_cl_number and cd_cl_number % 100 == 0:

# After
if cd_cl_number and should_exclude_job(cd_cl):
```

**변경 4 - delete_data 메서드:**
```python
# Before
if cd_number and cd_number % 100 == 0:

# After
if cd_number and should_exclude_job(cd):
```

**import 추가:**
```python
from utils.job_utils import should_exclude_job
```

---

## FP / 작업 구분

| 항목 | 내용 |
|------|------|
| 작업 유형 | 기능 작업 |
| 기능 점수 (FP) | 5 |
| 예상 공수 | 2H |

---

## 📢 공지용 요약

**[상세 내용]**
Dashboard에서 100단위 Job ID(CD100, CD200, CD300, CD400 등)가 표시되지 않도록 필터링 로직을 추가했습니다. 기존에는 mngr_sett_service.py에만 존재하던 100단위 제외 로직을 utils/job_utils.py로 공통화하여 여러 서비스에서 재사용할 수 있도록 개선했습니다. 중복 코드 제거로 유지보수성이 향상되었습니다.

**[요약]**
Dashboard 100단위 Job ID 필터링 및 공통 유틸리티 모듈화

---

Refs: REQ-2604-011

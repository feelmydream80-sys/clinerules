## Change Request

- CR ID        : REQ-2604-015
- 레파지토리   : msys
- 요청 유형    : fix
- 대상 스코프  : collection_schedule, mngr_sett
- 작업 유형    : 기능 작업
- 요청자       : 
- 요청 날짜    : 2026.04.20

---

### 지침 진행 과정

1. 00-core.md 확인 → "[작업 유형]"
2. 참조 문서: .clinerules/core/06.git-rules.md
3. 분석 내용: 수집 스케줄 색상 적용 로직 확인, 상태코드 정렬 확인

---

### 변경 요약 (1줄, 50자 이내)
> 수집 스케줄 CD907 색상 변경 및 상태별 설정 코드 정렬 개선

### 변경 배경 / 이유
> CD907(예정) 상태의 기본 배경색이 너무 밝아 가시성이 떨어지는 문제
> 관리자 설정의 상태별 표시에서 코드가 무작위 순서로 표시되어 사용성 저하

### 변경 전 → 변경 후
- Before: CD907 배경색 #f3f4f6 (밝은 회색), 상태코드 무작위 정렬
- After : CD907 배경색 #e5e7eb (진한 회색), 상태코드 오름차순 정렬

### 영향 범위
> - collection_schedule.js: CD907 기본 색상 값 변경
> - mngr_sett.js: 상태코드 렌더링 시 .sort() 적용

---

### 작업 상세 내역 (파일별)

| 순번 | 파일 경로 | 변경 유형 | 상세 내용 |
|------|-----------|-----------|-----------|
| 1 | `static/js/pages/collection_schedule.js` | 수정 | CD907 defaultColors 배경색 #f3f4f6 → #e5e7eb |
| 2 | `static/js/pages/mngr_sett.js` | 수정 | loadStatusCodes()에 statusCodes.sort((a, b) => a.cd.localeCompare(b.cd)) 적용 |

**변경 코드 요약:**
```javascript
// static/js/pages/collection_schedule.js
// Before
const defaultColors = {
    ...
    'CD907': { bg: '#f3f4f6', txt: '#6b7280' },
    ...
};

// After
const defaultColors = {
    ...
    'CD907': { bg: '#e5e7eb', txt: '#6b7280' },
    ...
};

// static/js/pages/mngr_sett.js
// Before
container.innerHTML = statusCodes.map(code => `...`).join('');

// After
container.innerHTML = statusCodes
    .sort((a, b) => a.cd.localeCompare(b.cd))
    .map(code => `...`).join('');
```

---

### FP / 작업 구분

| 항목 | 내용 |
|------|------|
| 작업 유형 | 기능 작업 |
| 기능 점수 (FP) | 2 |
| 예상 공수 | 0.5H |

---

### 공지용 요약

**[상세 내용]**
> 수집 스케줄 화면에서 CD907(예정) 상태의 기본 배경색을 밝은 회색에서 약간 더 진한 회색으로 변경하여 가시성 개선. 관리자 설정의 상태별 표시 설정 페이지에서 상태 코드가 오름차순으로 정렬되어 표시되도록 수정.

**[요약]**
> CD907 색상 개선 및 상태코드 정렬 개선

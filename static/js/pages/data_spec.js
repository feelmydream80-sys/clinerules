// static/js/pages/data_spec.js

/**
 * @file data_spec.js
 * @description 데이터 명세 페이지의 진입점(Entry Point) 역할을 합니다.
 * - `events.js` 모듈의 `initializePage` 함수를 호출하여 페이지를 초기화합니다.
 * 
 * @see {@link module:api} for API communication.
 * @see {@link module:ui} for UI rendering and DOM manipulation.
 * @see {@link module:events} for event handling and main application logic.
 */

import { initializePage as init } from '../modules/data_spec/events.js';

export { init };

## Change Request

- CR ID        : REQ-2604-018
- 레파지토리   : msys
- 요청 유형    : fix
- 대상 스코프  : routes, static/js/pages
- 작업 유형    : 기능 작업
- 요청자       : 사용자
- 요청 날짜    : 2026.04.21

---

## 📊 현재 상태
> 기존: /api/mngr_sett/status_codes와 /api/mngr_sett/schedule_settings API는 관리자만 접근 가능(@mngr_sett_required)
> 문제: 일반 사용자가 collection_schedule 페이지 접근 시 403 Forbidden 에러 발생
> 요구사항: 수집 현황 히트맵 색상 표시는 모든 인증 사용자가 조회 가능해야 함

## ✅ 완료된 것
> - [x] /api/mngr_sett/status_codes API에서 @mngr_sett_required 데코레이터 제거
> - [x] /api/mngr_sett/schedule_settings API에서 @mngr_sett_required 데코레이터 제거
> - [x] collection_schedule.js: 모든 사용자가 설정 API 호출하도록 수정

## 🔄 진행 중인 것
> - [ ] 없음

## ❓ 미결 결정사항
> - 없음

## 📋 다음 작업
> - [ ] 없음

## ⚠️ 주의사항 / 이전 실수
> - 설정 변경(POST/sync)은 여전히 관리자만 가능하도록 유지
> - 권한 변경 대상: GET 메서드만 (조회), POST/sync는 관리자 전용

## 📁 관련 파일 경로
| 구분 | 파일 경로 | 변경 유형 | 상세 내용 |
|------|-----------|-----------|-----------|
| Backend | `routes/mngr_sett_routes.py` | 수정 | @mngr_sett_required 제거 (status_codes, schedule_settings GET) |
| Frontend | `static/js/pages/collection_schedule.js` | 수정 | 모든 사용자가 API 호출하도록 isAdminUser 체크 제거 |

## 📝 상세 변경 내용

### Before / After

**routes/mngr_sett_routes.py - status_codes API**
```python
# Before
@api_mngr_sett_bp.route('/status_codes', methods=['GET'])
@login_required
@check_password_change_required
@mngr_sett_required  # ← 관리자만
def get_all_status_codes():

# After
@api_mngr_sett_bp.route('/status_codes', methods=['GET'])
@login_required
@check_password_change_required
def get_all_status_codes():
```

**routes/mngr_sett_routes.py - schedule_settings API**
```python
# Before
@api_mngr_sett_bp.route('/schedule_settings', methods=['GET'])
@login_required
@check_password_change_required
@mngr_sett_required  # ← 관리자만
def get_schedule_display_settings():

# After
@api_mngr_sett_bp.route('/schedule_settings', methods=['GET'])
@login_required
@check_password_change_required
def get_schedule_display_settings():
```

**collection_schedule.js**
```javascript
// Before
if (isAdminUser) {
    const statusCodesRes = await fetch('/api/mngr_sett/status_codes');
    // ...
}

// After
const statusCodesRes = await fetch('/api/mngr_sett/status_codes');
// isAdminUser 체크 없이 모든 사용자가 호출
```

---

## FP / 작업 구분

| 항목 | 내용 |
|------|------|
| 작업 유형 | 기능 작업 |
| 기능 점수 (FP) | 3 |
| 예상 공수 | 1H |

---

## 📢 공지용 요약

**[상세 내용]**
> 관리자 설정 API 조회 권한을 모든 인증 사용자에게 개방했습니다.
> - /api/mngr_sett/status_codes (GET): @mngr_sett_required → login_required
> - /api/mngr_sett/schedule_settings (GET): @mngr_sett_required → login_required
> - 설정 변경(POST/sync)은 여전히 관리자만 가능
> - collection_schedule 페이지에서 카드 색상이 정상 표시됨

**[요약]**
> 관리자 설정 API 조회 권한 개방 (REQ-2604-018)
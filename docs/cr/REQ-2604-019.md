## Change Request

- CR ID        : REQ-2604-019
- 레파지토리   : msys
- 요청 유형    : fix
- 대상 스코프  : card_summary, data_definition, job_utils
- 작업 유형    : 기능 작업
- 요청자       : (사용자)
- 요청 날짜    : 2026.04.21

---

## 📊 현재 상태
> 변경 전 문제 상황, 버그 현상, 개선 필요성

1. **CD1000 그룹 명칭 버그**: /card_summary에서 CD1000 그룹의 Job(예: CD1001) 표시 시 solcast(CD100 명칭)으로 잘못 출력, comp(CD1000 명칭)가 되어야 함
2. **10만 단위 입력 가능**: 관리자 설정 > 그룹 추가에서 CD10000(5자리) 이상 입력 가능하여将来の 시스템 문제 발생 가능성

---

## ✅ 완료된 것
> - [x] card_summary.js: 4자리 Job ID 그룹 코드 변환 버그 수정
> - [x] card_summary_service.py: 프론트엔드와 동일한 그룹 로직으로 통일
> - [x] job_utils.py: validate_job_id_format() 함수 추가 (10만 단위 검증)
> - [x] data_definition_api.py: 백엔드 Job ID 검증 추가
> - [x] dataDefinition.js: cd_cl, cd 실시간 검증 로직 추가
> - [x] modules/ui.js: newGroupCdMaxError, newGroupCdClMaxError 안내글 추가

---

## 🔄 진행 중인 것
> - [ ] 없음 (완료)

---

## ❓ 미결 결정사항
> 없음

---

## 📋 다음 작업
> - [ ] 없음

---

## ⚠️ 주의사항 / 이전 실수
> - 기존 하드코딩된 그룹 코드 변환 로직은 3자리만 고려하여 4자리(CD1000)에서 버그 발생
> - 백엔드와 프론트엔드의 로직 불일치로 인한 문제

---

## 📁 관련 파일 경로
| 구분 | 파일 경로 | 변경 유형 | 상세 내용 |
|------|-----------|-----------|-----------|
| Frontend | `static/js/pages/card_summary.js` | 수정 | getGroupDisplayName() 4자리 처리 로직 수정 |
| Backend | `service/card_summary_service.py` | 수정 | 그룹 결정 로직 프론트엔드와 동일하게 수정 |
| Backend | `utils/job_utils.py` | 수정 | validate_job_id_format() 함수 추가 |
| Backend | `routes/api/data_definition_api.py` | 수정 | create_data() 검증 추가 |
| Frontend | `static/js/tabs/dataDefinition/dataDefinition.js` | 수정 | validateAddGroupModal() cd_cl/cd 검증 추가 |
| Frontend | `static/js/tabs/dataDefinition/modules/ui.js` | 수정 | newGroupCdMaxError, newGroupCdClMaxError 안내글 추가 |

---

## 📝 상세 변경 내용

### Before / After

**파일: `static/js/pages/card_summary.js`**
```javascript
// Before (버그)
if (numericPart.length >= 3) {
    groupJobId = baseJobId.substring(0, 4) + '0';  // CD101 -> CD100, CD1001 -> CD100 오류!
}

// After (수정)
if (numericPart.length >= 4) {
    groupJobId = 'CD' + numericPart.substring(0, 2) + '00';  // CD1001 -> CD1000 ✓
} else if (numericPart.length >= 3) {
    groupJobId = 'CD' + numericPart.substring(0, 1) + '00'; // CD101 -> CD100 ✓
} else {
    groupJobId = 'CD' + numericPart + '00';
}
```

**파일: `service/card_summary_service.py`**
```python
# Before
if len(numeric_part) == 4:
    group = f"CD{numeric_part[:2]}00"
else:
    group = f"CD{numeric_part[0]}00"

# After (수정)
if len(numeric_part) >= 4:
    group = f"CD{numeric_part[:2]}00"  # CD1001 -> CD1000
elif len(numeric_part) >= 3:
    group = f"CD{numeric_part[0]}00"    # CD101 -> CD100
else:
    group = f"CD{numeric_part}00"        # CD12 -> CD1200
```

**파일: `utils/job_utils.py` (신규 함수)**
```python
def validate_job_id_format(job_id):
    """Job ID 형식 및 범위 검증 - CD1 ~ CD9999까지만 허용"""
    if not job_id:
        return False, "Job ID가 필요합니다."

    match = re.match(r'CD(\d+)', str(job_id).upper())
    if not match:
        return False, "Job ID는 'CD'로 시작하고 숫자가 따라와야 합니다."

    cd_number = int(match.group(1))
    if cd_number >= 10000:
        return False, "그룹 코드는 CD1 ~ CD9999까지만 입력 가능합니다. (4자리 이하)"

    return True, None
```

**파일: `modules/ui.js` (안내글 추가)**
```html
<!-- After -->
<div id="newGroupCdClMaxError" style="display: none;">
    그룹 코드는 100단위로 CD100 ~ CD9900까지만 입력 가능합니다.
</div>
<div id="newGroupCdMaxError" style="display: none;">
    데이터 코드는 CD1 ~ CD9999까지만 입력 가능합니다. (4자리 이하)
</div>
```

---

## FP / 작업 구분

| 항목 | 내용 |
|------|------|
| 작업 유형 | 기능 작업 |
| 기능 점수 (FP) | 3 |
| 예상 공수 | 2H |

---

## 📢 공지용 요약

**[상세 내용]**
1. CD1000 그룹 명칭 표시 버그 수정: card_summary에서 CD1001 → CD1000 그룹 매핑 시 solcast 대신 comp 표시
2. 10만 단위(CD10000) 이상 그룹/데이터 코드 입력 방지: 프론트엔드 실시간 검증 + 백엔드 API 검증 추가
3. 관리자 설정 > 그룹 추가 시 4자리 이하만 입력 가능하도록 제한

**[요약]**
- CD1000 그룹 명칭 버그 수정
- 5자리(10만) 이상 입력 차단
- 실시간 입력 검증 구현
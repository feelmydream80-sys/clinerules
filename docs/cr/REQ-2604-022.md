## Change Request

- CR ID        : REQ-2604-022
- 레파지토리   : msys (wordcloud)
- 요청 유형    : fix
- 대상 스코프  : Frontend (dateUtils, userList, index.js, html)
- 작업 유형    : 기능 작업
- 요청자       : 사용자
- 요청 날짜    : 2026.04.28

---

### 변경 요약 (1줄, 50자 이내)
> Fix line chart to show weekly data matching heatmap

### 변경 배경 / 이유
> 히트맵(막대)과 선차트가 같은 데이터를 사용하지만 표현 방식이 달라 다른 추세로 보임.
> 사용자가 월별 합산이 아닌 주차별 값으로 선차트를 표시해달라고 요청.

### 변경 전 → 변경 후
- Before: 선차트 - 월별 합산 (예: 1월=100, 2월=120)
- After : 선차트 - 주차별 값 (예: 1월1주=25, 1월2주=30...), 히트맵과 동일하게 주차별 표시

### 영향 범위
> 사용자접속정보 목록의 선차트에만 영향

---

### 작업 상세 내역 (파일별)

| 순번 | 파일 경로 | 변경 유형 | 상세 내용 |
|------|-----------|-----------|-----------|
| 1 | `static/js/modules/common/dateUtils.js` | 수정 | getLast6Months(), getWeeksPerMonthFn() 함수 추가 |
| 2 | `static/js/tabs/userAccessInfo/userList.js` | 수정 | _renderWeeklyLineCells()를 주차별 표시로 변경 |
| 3 | `static/js/tabs/userAccessInfo/index.js` | 수정 | getLast6Months import 및 updateMonthHeaders() 수정 |
| 4 | `templates/mngr_sett.html` | 수정 | thead에 month-header-col 클래스 추가 |

**변경 코드 요약:**
```javascript
// userList.js - Before (월별 합산)
const monthSums = [];
weeksPerMonth.forEach(weekCount => {
    let sum = 0;
    for (let i = 0; i < weekCount; i++) {
        sum += cleanWeeklyData[weekIndex] || 0;
    }
    monthSums.push(sum);
});

// After (주차별 데이터)
weeksPerMonth.map((weekCount, monthIdx) => {
    const monthWeeks = [];
    for (let i = 0; i < weekCount; i++) {
        monthWeeks.push(cleanWeeklyData[weekIndex]);
    }
    // 각 월의 주차별 선차트 렌더링
});
```

---

### FP / 작업 구분

| 항목 | 내용 |
|------|------|
| 작업 유형 | 기능 작업 |
| 기능 점수 (FP) | 2 |
| 예상 공수 | 0.25NH |

---

### 공지용 요약

**[상세 내용]**
> 사용자접속정보의 선차트가 월별 합산에서 주차별 값으로 변경되어 히트맵과 동일한 추세를 보입니다.
> 추가로 dateUtils.js에 공통 날짜 함수(getLast6Months, getWeeksPerMonthFn)를 추가했습니다.

**[요약]**
> 선차트 표시 개선
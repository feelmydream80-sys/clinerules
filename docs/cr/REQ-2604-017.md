## Change Request

- CR ID        : REQ-2604-017
- 레파지토리   : msys
- 요청 유형    : fix
- 대상 스코프  : static/js/pages
- 작업 유형    : 기능 작업
- 요청자       : 사용자
- 요청 날짜    : 2026.04.21

---

### 지침 진행 과정

1. 00-core.md 확인 → "기능 문제 분석/디버깅"
2. 참조 문서: 03.workflow.md
3. 분석 내용: e.txt 에러 로그 분석으로 문제 원인 파악

---

### 변경 요약 (1줄, 50자 이내)
> 게스트 사용자権限 문제 및 상태 코드 초기화 버그 수정

### 변경 배경 / 이유
>게스트 사용자가 collection_schedule 페이지 접근 시 403 Forbidden 에러 발생
>- /api/mngr_sett/schedule_settings API 호출 시 403
>- /api/mngr_sett/status_codes API 호출 시 403
>- getStatusInfoByCd('CD901') 호출 시 TypeError: Cannot read properties of undefined

### 변경 전 → 변경 후
- Before: 게스트도 관리자 전용 API 호출, _statusMapByCd 미초기화
- After: 관리자만 API 호출, _statusMapByCd {} 초기화

### 영향 범위
> collection_schedule 페이지 게스트 사용자

---

### 작업 상세 내역 (파일별)

| 순번 | 파일 경로 | 변경 유형 | 상세 내용 |
|------|-----------|-----------|-----------|
| 1 | `static/js/pages/collection_schedule.js` | 수정 | isAdminUser 모듈 레벨 변수 추가 |
| 2 | `static/js/pages/collection_schedule.js` | 수정 | init() 내 let 제거, 값 할당만 수행 |
| 3 | `static/js/pages/collection_schedule.js` | 수정 | memo-btn 조건부 HTML 생성 (게스트: display: none) |
| 4 | `static/js/pages/collection_schedule.js` | 수정 | schedule_settings API isAdminUser 체크 추가 |
| 5 | `static/js/pages/collection_schedule.js` | 수정 | status_codes API isAdminUser 체크 추가 |
| 6 | `static/js/pages/collection_schedule.js` | 수정 | updateMemoButtons 게스트 분기 처리 |
| 7 | `static/js/pages/collection_schedule.js` | 수정 | _statusMapByCd: {} 초기화 추가 |

**변경 코드 요약:**
```javascript
// Line 13 - isAdminUser 추가
let isAdminUser = false;

// Line 102 - _statusMapByCd 초기화
_statusMapByCd: {}

// Line 571-573 - 메모 버튼 조건부 생성
const memoBtnHtml = isAdminUser
    ? `<span class="memo-btn" ...>+</span>`
    : `<span class="memo-btn" ... style="display: none;">+</span>`;

// Line 1473-1476 - 게스트 분기 처리
if (isAdminUser) {
    btn.textContent = '+';
    btn.style.display = '';
} else {
    btn.style.display = 'none';
}
```

---

### FP / 작업 구분

| 항목 | 내용 |
|------|------|
| 작업 유형 | 기능 작업 |
| 기능 점수 (FP) | 3 |
| 예상 공수 | 1.5H |

---

### 공지용 요약

**[상세 내용]**
> collection_schedule 페이지에서 게스트(비관리자) 사용자가 접근 시 발생하는 버그를 수정했습니다.
> 1) 관리자 전용 API(/api/mngr_sett/*) 호출 시 403 에러 발생 문제 - isAdminUser 권한 체크 추가
> 2) settingsManager._statusMapByCd 미초기화로 인한 TypeError 버그 - {} 초기화 추가
> 3) 메모 버튼 표시 로직 개선 - 게스트는 메모 없으면 숨김, 있음则 ✓ 표시

**[요약]**
> 수집 일정 페이지 게스트 권한 버그 수정 (REQ-2604-017)
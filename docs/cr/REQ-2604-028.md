## Change Request

- CR ID        : REQ-2604-028
- 레파지토리   : msys (D:\dev\msys)
- 요청 유형    : feat
- 대상 스코프  : templates, static/js/pages, static/js/modules/mngr_sett, static/js/tabs
- 작업 유형    : 기능 작업
- 요청자       : 사용자
- 요청 날짜    : 2026.04.29

---

## 📊 현재 상태
> 변경 전 문제 상황, 버그 현상, 개선 필요성
- 관리자 설정 페이지 접속 시 전체 화면 반투명 검정 오버레이(`adminLoadingOverlay`) 표시
- 다른 페이지(jandi.js, data_report.js 등)와 로딩 방식 불일치 (타 페이지는 인라인/테이블 내 로딩)
- 3개 API 호출이 순차 실행되어 로딩 시간이 길고, 빠른 네비게이션 시 새로고침처럼 보임

---

## ✅ 완료된 것
> - [x] mngr_sett.html에서 전체 오버레이(`adminLoadingOverlay`) 제거
> - [x] 각 테이블 tbody에 인라인 로딩 행 추가 (settings/icon/user)
> - [x] mngr_sett.js의 loadPageData 함수를 순차 호출에서 Promise.allSettled 병렬 호출로 변경
> - [x] events.js 내 모든 adminLoadingOverlay 참조 제거 및 토스트 알림으로 대체
> - [x] popupManagement.js 내 모든 adminLoadingOverlay 참조 제거 및 토스트 알림으로 대체

---

## 🔄 진행 중인 것
> - [ ] 없음

---

## ❓ 미결 결정사항
> - 없음

---

## 📋 다음 작업
> - [ ] 없음

---

## ⚠️ 주의사항 / 이전 실수
> - 이전 작업에서 CR 보고서 및 상세 커밋 메시지 작성을 누락 (지침 미준수)
> - 본 작업부터 06.git-rules.md 지침 엄격 준수

---

## 📁 관련 파일 경로
| 구분 | 파일 경로 | 변경 유형 | 상세 내용 |
|------|-----------|-----------|-----------|
| Frontend | `templates/mngr_sett.html` | 수정 | 전체 오버레이 제거, 3개 tbody에 인라인 로딩 행 추가 |
| Frontend | `static/js/pages/mngr_sett.js` | 수정 | loadPageData 함수 병렬 호출로 변경, 오버레이 참조 제거 |
| Frontend | `static/js/modules/mngr_sett/events.js` | 수정 | 7개 함수에서 오버레이 참조 제거, 토스트 알림 추가 |
| Frontend | `static/js/tabs/popupManagement.js` | 수정 | 2개 함수에서 오버레이 참조 제거, 토스트 알림 추가 |

---

## 📝 상세 변경 내용

### Before / After

**파일: `templates/mngr_sett.html`**
```html
<!-- Before -->
<div id="adminLoadingOverlay" class="fixed inset-0 bg-gray-800 bg-opacity-75 flex items-center justify-center z-50 hidden">
    ...
</div>
<tbody id="settingsTableBody"><!-- 설정 데이터가 여기에 로드됩니다 --></tbody>

<!-- After -->
<tbody id="settingsTableBody">
    <tr id="settings-loading-row">
        <td colspan="20" class="text-center py-4 text-gray-500">데이터를 로드하는 중입니다...</td>
    </tr>
</tbody>
```

**파일: `static/js/pages/mngr_sett.js`**
```javascript
// Before
const loadingOverlay = container.querySelector('#adminLoadingOverlay');
if (loadingOverlay) loadingOverlay.classList.remove('hidden');
try {
    let adminSettingsResult = await getAdminSettings(...);
    let icons = await getIcons();
    let userData = await fetchUsersAndMenus();
    // render...
} finally {
    if (loadingOverlay) loadingOverlay.classList.add('hidden');
}

// After
try {
    const [adminResult, iconsResult, userResult] = await Promise.allSettled([
        getAdminSettings(...),
        getIcons(),
        fetchUsersAndMenus()
    ]);
    // 각 결과 처리...
}
```

---

## FP / 작업 구분
| 항목 | 내용 |
|------|------|
| 작업 유형 | 기능 작업 |
| 기능 점수 (FP) | 3 |
| 예상 공수 | 0.5H |

---

## 📢 공지용 요약

**[상세 내용]**
> 관리자 설정 페이지의 로딩 UI를 전체 오버레이에서 테이블 인라인 로딩으로 변경하여 다른 페이지와 일관성을 맞추고, API 호출을 병렬로 처리하여 로딩 시간을 단축함.

**[요약]**
> 관리자 설정 로딩 UI 개선 (오버레이 제거 + 병렬 호출)

---

Refs: REQ-2604-028
FP: 3 | 공수: 0.5H | 작업유형: 기능
